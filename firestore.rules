rules_version = '2';
service cloud.firestore {
  match /databases/{db}/documents {

    match /papers/{id} {
      allow read, write: if request.auth != null;
    }

    match /counters/{id} {
      allow read, write: if request.auth != null;
    }
  }
}
